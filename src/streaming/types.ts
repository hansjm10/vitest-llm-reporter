/**
 * Streaming Types
 *
 * Simple types for streaming functionality
 *
 * @module streaming/types
 */

/**
 * Streaming modes supported by the reporter
 */
export type StreamingMode = 'stdout' | 'stderr' | 'both' | 'none'
